<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SupplierRequestsApp.Presentation.Pages.StoragesPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Text="Добавить склад"
                    Clicked="OnAddNewStorageClicked"
                    HorizontalOptions="Center"
                    Grid.Row="0" />

            <Grid Grid.Row="1" Padding="10" BackgroundColor="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <Label Text="ID" FontAttributes="Bold" Grid.Column="0" />
                <Label Text="Продуктов на складе" FontAttributes="Bold" Grid.Column="1" />
                <Label Text="" Grid.Column="2" />
                <Label Text="Действия" FontAttributes="Bold" HorizontalTextAlignment="End" Grid.Column="3" />
            </Grid>

            <CollectionView x:Name="StorageCollectionView"
                            ItemsSource="{Binding Storages}"
                            Grid.Row="2">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="80" />
                            </Grid.ColumnDefinitions>
                            <Label Text="{Binding StorageId}" Grid.Column="0" />
                            <Label Text="{Binding Products.Count}" Grid.Column="1" />
                            <Label Text="" Grid.Column="2" />
                            <Button Text="Изменить" Grid.Column="3" HorizontalOptions="End" Clicked="OnEditClicked" CommandParameter="{Binding}" />
                            <Button Text="X" Grid.Column="4" BackgroundColor="DarkRed" TextColor="White" HorizontalOptions="End" Clicked="OnRemoveClicked" CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage>
